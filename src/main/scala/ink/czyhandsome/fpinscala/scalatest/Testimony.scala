package ink.czyhandsome.fpinscala.scalatest

/**
  * $DESC
  *
  * @author 曹子钰, 2019-01-06
  */
object Testimony {
/*  def main(args: Array[String]): Unit = {
    val ints = Gen.listOf(Gen.choose(0, 100))
    val prop = forAll(ints) { ns => ns.reverse.reverse == ns } &&
      forAll(ints) { ns => ns.headOption == ns.reverse.lastOption } &&

    val fail = forAll(ints) { ns => ns.reverse == ns }
    prop.check()
    fail.check()
  }*/
}
